CREATE TABLE IF NOT EXISTS notes (
    note_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    note TEXT NOT NULL,
    active INTEGER DEFAULT 1,
    character INTEGER REFERENCES characters(character_id) ON DELETE CASCADE NOT NULL
);